/* const colors = ['red', 'orange', 'yellow', 'green', 'blue', 'indigo', 'violet'];
const clases = ['a','b','c','d','e','f','g'];
for (i = 0 ; i < clases.length ; i++){
    const spans = document.querySelector(`.${clases[i]}`)
    for(j = 0 ; j < i; j++){
    }
   spans.style.color = `${colors[j]}`
}
 */

